get:
  tags: [Система]
  summary: Статус API
  security: []
  parameters:
    - name: X-API-Version
      in: header
      description: Версия API
      schema:
        type: string
        enum: [v1, v2]
        default: v1
  responses:
    '200':
      description: Статус системы
      content:
        application/json:
          schema:
            type: object
            properties:
              status:
                type: string
                example: "OK"
              version:
                type: string
                example: "1.0.0" 